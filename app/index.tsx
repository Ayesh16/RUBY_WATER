import { Text, View } from "react-native";
import Home from "@/componets/Home";

export default function Index() {
  return (
    <View
      style={{
        flex: 1,
      }}
    >
      <Home />
    </View>
  );
}
