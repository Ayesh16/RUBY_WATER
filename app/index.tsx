import { Text, View } from "react-native";
import Login from '../componets/Login'

export default function Index() {
  return (
    <View
      style={{
        flex: 1,
      }}
    >
      <Login />
    </View>
  );
}
